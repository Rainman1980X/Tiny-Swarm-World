==  Risks & Technical Debt

Potential challenges and risks.

=== Remaining Command Execution Risk

The command catalog classifies command metadata and enforces workflow
allow-lists before command building when product command YAML exists. The
current Multipass-removal baseline intentionally has no product
`command_*.yaml` files. Retained VM-named command-builder helpers are legacy
command-template infrastructure and must not be treated as supported Multipass
provider behavior. Shell command replacement with argv-style execution remains
future hardening work if a command catalog is reintroduced.

Command parameters are constrained by type-specific patterns before
substitution. Runner logs no longer include the full raw command string,
stdout, or stderr payloads. Verification evidence rejects raw or sensitive keys
and values before persistence.

Remaining debt:

* the runner still uses shell execution for command-template infrastructure
  when a catalog is supplied;
* Issue #188's five selected adapter targets now use the shared bounded runner;
  remaining direct process sites are explicit compatibility, provider, host,
  network, service, or tooling boundaries and must not be silently absorbed by
  the generic runner;
* the Issue #188 before/after inventories and architecture guard record the
  remaining exceptions and reject new unapproved direct process spawning in
  production infrastructure;
* the local quality gate currently has one independent governance exception:
  the workflow-create update to `documentation/arc42/08_concepts.adoc` needs
  its cached skill-registry hash reconciled before the full repository gate is
  green;
* no current product command catalog exists, so any reintroduced catalog must
  restore non-vacuous command-backed verification coverage;
* composed mutating platform workflows now block with explicit target IDs and
  operator-facing reasons before apply, but still need concrete command-backed
  verification contracts before they can safely execute live steps;
* the host-neutral desired inventory baseline is committed and aligned with
  the default service-access setup profile, but observed inventory collection
  and reconciliation are not yet wired into platform verification workflows;
* removed legacy scripts remain historical live-operation surfaces outside the
  workflow-level CLI consent guard;
* reset/destroy teardown is wired for configured managed LXC-native nodes, but
  live operator validation, retention hardening, and unmanaged-resource
  recovery guidance remain open;
* artifact and deployment workflows are now explicit composition contracts;
  artifact workflows have tested Nexus repository and setup image-publication
  contracts, and Issue #232 now adds profile-aware static contract preflight,
  bounded readiness gating, safe evidence and fail-closed setup sequencing;
* Issue #232 live registry, Nexus, Docker and end-to-end installation evidence
  remains an operator-consent-gated risk. Local quality and mocked readiness
  tests do not establish `LIVE_VERIFIED` status;
* `artifacts prepare` and `deployment apply` are live-consent gated before
  service construction, but the approved live behavior behind those gates is
  still technical debt.

=== Autonomous Setup Safety Debt

The autonomous setup safety contract is accepted. The canonical `setup run`
workflow-level orchestrator now exists and fails closed behind live consent,
but end-to-end full runnable setup remains incomplete.

Remaining debt:

* `setup run` now includes the service-access management stack in the full
  profile, but still needs complete end-to-end verification hardening;
* setup preflight and setup manifest contracts exist, but still need live
  profile hardening for resource, credential, and readiness handoff detail;
* command-backed verification remains incomplete for live Platform phases;
* Artifact readiness contracts now cover image publication prerequisites and
  registry/Nexus readiness targets; authorized live observation and image
  publication evidence remain outstanding. Deployment readiness still needs
  container and service health observed-state contracts;
* Portainer, Nexus, Jenkins, Apache Pulsar, SonarQube, and Swagger/NGINX service
  readiness must not be claimed until verified through tests or explicit live
  smoke evidence;
* Apache Pulsar may require tighter memory/CPU guidance after live smoke
  validation;
* Pulsar standalone mode is sufficient for local greenpath validation, but it
  is not equivalent to a production Pulsar cluster;
* Pulsar Admin API uses internal port `8080`, so host exposure must continue to
  use `8087` or service-access routing to avoid collisions;
* direct preparation scripts and host-side orchestration scripts have been
  removed, and legacy helper reintroduction still needs
  migration or clear legacy retirement;
* automatic host package installation and direct script promotion need future
  ADRs before they can change the accepted safety model; explicit
  non-interactive live consent is governed separately by
  `adr-explicit-non-interactive-live-consent.adoc`.

=== LXC Native Provider Migration Risks

The LXC-native provider direction is accepted and partially implemented.
Default provider selection, Incus readiness, node lifecycle adapters,
Docker Engine setup inside managed LXC nodes, Swarm bootstrap, setup cluster
phase integration, structured managed-manager membership verification,
LXC-native managed-node reconcile completion, and unsupported provider
rejection are implemented and test-backed.

Remaining risks and debt:

* Issue #183's local compatibility-preserving split is implemented through
  `infrastructure/adapters/clients/lxc/` and direct composition imports. The
  legacy module is now a thin compatibility facade. Remaining debt is the
  failing external SonarCloud gate and missing workflow-commit smell
  comparison. Live browser acceptance evidence is now present, but the issue
  must remain blocked until external quality is resolved and audited.
* default artifact and deployment workflows have provider-native publication,
  stack, and observed-state contracts, but live registry, Portainer, Swarm,
  external-input, and readiness evidence still needs explicit operator
  validation;
* stale direct instance-level `tsw-proxy-*` devices from older runs can still
  block reset or setup until the operator runs the explicit
  `platform repair-lxc-proxy-drift` workflow or manually resolves the named
  provider state;
* Docker Swarm inside Incus containers has not been live-validated by this
  repository workflow;
* local tests verify phase ownership and structured managed-manager evidence,
  but they do not prove that a live Incus manager reports the expected
  `Leader` state or that all configured nodes can communicate on a real
  overlay network;
* WSL2 success could be overclaimed if documentation, static tests, or native
  Linux evidence are treated as WSL2 live evidence;
* Docker Swarm inside Incus containers may require nesting, cgroup,
  AppArmor, seccomp, capability, privileged-profile, or host-mount decisions
  that need security and DevOps review;
* automatic provider installation, daemon repair, group membership changes,
  and bridge/firewall mutation remain outside the accepted default behavior;
  non-interactive live consent is supported only through the explicit,
  per-invocation contract in
  `adr-explicit-non-interactive-live-consent.adoc`.

=== Service Access Dashboard And Vaultwarden Baseline Risks

The service-access dashboard and Vaultwarden baseline is implemented as
repository compose assets, image-packaged dashboard/NGINX assets, setup
profile contracts, setup manifest requirements and static tests.
The current central `http://localhost` route requires a fresh
provider-specific redeployment before it is claimed as verified.

Remaining risks and debt:

* credential leakage could occur through dashboard content, compose defaults,
  logs, evidence, screenshots or exported artifacts if password values are
  duplicated outside Vaultwarden;
* required setup password values must remain operator-supplied; committed
  static defaults are not accepted as preflight secret-value sources;
* Vaultwarden admin-token ownership, rotation, persistence, backup, restore
  and rollback are not implemented and remain blockers before production-like
  claims; the committed default is only the external Swarm secret name, never
  the token value;
* missing Vaultwarden external Swarm input now blocks before service-access
  stack deployment, but creating or rotating that input remains an operator
  responsibility outside the default quality gate;
* a bootstrap cycle is possible if required setup credentials are moved into
  Vaultwarden before Vaultwarden itself is reachable;
* service reachability could be falsely presented as healthy without fresh
  observed-state or HTTP evidence; current dashboard assets render catalog
  reachability as unknown unless evidence is collected separately;
* generated effective-access-model JSON and static browser matrices could be
  overclaimed as live proof; they record configured routes and test evidence,
  not DNS, TLS, HTTP, Swarm-readiness, or authenticated-login observations;
* the central `http://localhost` service-access route and local `https://localhost` Vaultwarden route are implemented in
  repository assets but still needs a fresh live redeployment and browser smoke
  test before it can be claimed as verified;
* local registry availability and image publication still need live
  observed-state hardening beyond the static artifact contracts;
* plain HTTP access is acceptable only as a local development baseline until a
  later ADR accepts public certificate automation or wider exposure controls;
* scope could drift into a React/browser frontend project or Traefik routing
  without the required ADR and quality-gate coverage.

Resolved or narrowed risks:

* the published port collision with Swagger/NGINX port `80` is resolved by
  making service-access the owner of `80` and moving Swagger/NGINX to `8084`;
* normal proxy exposure no longer creates direct instance-level `tsw-proxy-*`
  devices; expected proxy state belongs to the manager-only LXC profile;
* direct instance-level project proxy devices remain drift and are repairable
  only through the explicit profile-equivalence-gated repair workflow;
* dashboard and NGINX repository assets are image-packaged for the
  Portainer-managed stack path instead of bind-mounted from the repository;
* static tests reject React/Vite project files, live orchestration helpers,
  credential-bearing dashboard links, and raw password rendering.
* one effective-model port now supplies Traefik labels, dashboard links,
  health targets, productive routing evidence and browser expectations;
* shared-upstream app/API labels coexist, and active conditional routes are no
  longer also reported as skipped;
* routing evidence uses a fixed credential-value-free projection and atomic
  local replacement before stack apply;
* renderer output is the dashboard-test authority with an explicit committed
  fallback drift test;
* browser suite membership is dynamic, and missing evidence is an explicit
  non-success state.
* service-access live evidence must be collected separately for the default
  `lxc_native` provider baseline.

=== SOLID Refactor Chain (#189 -> #184 -> #191 -> #187 -> #190 -> #192 -> #186)

As of 2026-08-09, Issues #189 and #184 have completed their local
implementation slices on their serialized execution branches.
S189-03 and S184-03 record the after-inventories, local quality evidence and
independent completion handoffs. These are local implementation statements
only; they do not claim live, browser or external-gate verification.

The current baseline includes the Issue #238 LXC package split, including
candidate service-wrapper and stack-prerequisite modules. The indexed
workflows therefore require residual inventories and compatibility audits
before adding code. The chain keeps the following ownership order:

* #189 established the authoritative LXC backend CLI and shared utility
  boundary locally and passed its S189-03 audit;
* #184 decomposes node-provider lifecycle responsibilities while reusing that
  boundary and passed its S184-03 audit;
* #191 froze typed, serialized verification-evidence construction locally and
  passed its compatibility and audit gates;
* #187 isolates HostPreflightProbe service matching behind a registry locally
  and passed its compatibility and audit gates;
* #190 completes residual stack prerequisite and asset-transfer strategy
  dispatch locally and passed its compatibility and audit gates;
* #192 verifies LXC Portainer/Nexus wrapper ownership and compatibility locally
  and passed its compatibility and audit gates; and
* #186 audited the claimed global DI scope, verified explicit composition
  bindings, added a regression guard and completed with a bounded no-op;

The current read-only baseline scan found no `infra_core_container`,
`infra_core_di_*`, DI decorator package or `infrastructure/dependency_injection`
path. Issue #186 therefore has a mandatory repository-wide audit and bounded
no-op decision; it must not invent a new Service Locator or container when
composition is already explicit.

The indexed workflow remains serialized because the issues share LXC command,
node-provider, evidence, preflight, Swarm, service-wrapper and composition
locks. Local verification remains the authoritative default. Live LXC/Incus,
Docker/Swarm, Portainer/Nexus, browser/Selenium and SonarQube checks remain
opt-in and must use the verification-state policy with observable redacted
evidence. The #189 local quality pass is not live or external evidence.


=== RC1 Evidence Completion Follow-up (2026-09-12)

The remaining RC1 evidence requirements are governed by the indexed workflow
`documentation/workflow/workflow.index.md` and its issue-local slices. The
workflow keeps WSL2 and native-Linux validation separate, freezes the exact
candidate before live execution, serializes shared target and release evidence,
and preserves `LIVE_*` and external-gate non-success states. R05 is a
read-only reconciliation of already verified hosted evidence. The workflow
must not convert local, historical, queued, skipped, blocked or unavailable
results into RC1 acceptance.
